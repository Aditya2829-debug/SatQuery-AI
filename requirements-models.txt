# Inference dependencies only; pip install -e . separately.
torch>=2.6
torchvision>=0.21
pillow>=10.4
open_clip_torch>=2.26
transformers>=4.57,<5
accelerate>=1.0
peft>=0.17,<1
# Required by the historical evaluation/training scripts.
qwen-vl-utils>=0.0.14
